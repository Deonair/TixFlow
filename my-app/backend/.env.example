########################################
# Server / Environment
########################################
PORT=5050
# Zet in productie altijd NODE_ENV=production
NODE_ENV=production

# Database
# In productie: verplicht een echte URI (Atlas of managed instance)
# In development: mag lokaal draaien, default is 127.0.0.1
MONGO_URI=mongodb://127.0.0.1:27017/tixflow
SESSION_SECRET=please-change-in-production
COOKIE_SAMESITE=lax # opties: lax | strict | none
COOKIE_SECURE=auto  # opties: true | false | auto
########################################
# Fallback beleid
########################################
# Development-only flags. In productie worden deze genegeerd tenzij anders vermeld.
USE_IN_MEMORY_MONGO=false      # dev-only: forceer direct in-memory (geen externe Mongo)
DEV_IN_MEMORY_FALLBACK=false   # dev-only: val naar in-memory bij connectiefout
PROD_IN_MEMORY_FALLBACK=false  # productie: alleen true bij noodgeval; anders faalt de app hard

# App base URL (voor redirects en ticket links)
APP_BASE_URL=https://tixflow.nl

# E-mail (Resend)
RESEND_API_KEY=your_resend_api_key_here
EMAIL_FROM=tickets@send.tixflow.nl

# Stripe
STRIPE_SECRET_KEY=sk_test_your_key
STRIPE_WEBHOOK_SECRET=whsec_your_secret
