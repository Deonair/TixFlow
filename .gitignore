# Allow committing lockfiles (needed for CI caching and npm ci)
# Root lockfile
# package-lock.json
# Backend lockfile
# my-app/backend/package-lock.json

# package.json

# Ignore root node_modules
node_modules/

# Ignore environment files at root
.env
.env.local
.env.*.local

# Optional: editor/system files
.DS_Store
.vscode/
.idea/
# Ignore local GPT notes
GPT/
